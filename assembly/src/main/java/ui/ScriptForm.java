package ui;

/**
 * A simple window with a form, taking a script name and options map, and loading to edit the options for the script
 * When clicking the "Run" button, it will return a run flag and updated options map to the caller, which can then run the script with the updated options.
 * 
 * Script information are to be retrieved from the simpleApi functions
 */
public class ScriptForm {
    
}
